syntax = "proto3";

service NoticiaService {
    rpc List (Empty) returns (NoticiaList) {}
}

message Empty {}

message Noticia {
  string  titulo = 1;
  string  texto = 2;
  string  data = 3;
  string  link = 4;
}
message NoticiaList {
   repeated Noticia noticia = 1;
}